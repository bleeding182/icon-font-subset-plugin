- :plugin - analyzes Kotlin source for icon usage and invokes native HarfBuzz to emit a subsetted font + generated constants
- :runtime - Compose Glyph/GlyphFont backed by HarfBuzz JNI outline extraction

- Native libs for the plugin cross-compile via `cd plugin && ./build-in-docker.sh`
